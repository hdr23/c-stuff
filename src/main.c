/*
 * main.c — Your sandbox. 
 *
 * Build: from project root:
 *   mkdir build && cd build && cmake .. && cmake --build .
 * Run:   ./sandbox   (or sandbox.exe on Windows)
 */

#include <stdio.h>

int main(void)
{
    return 0;
}
